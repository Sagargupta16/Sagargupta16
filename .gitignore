# VS Code settings
.vscode/
!.vscode/settings.json  # Keep settings but ignore others

# Markdown linting configuration (keep local)
.markdownlint.json

# AI assistant config (personal, not for public repo)
CLAUDE.md

# Temporary files
*.tmp
*.temp
*.log
*.cache
*.bak
*.old
*~
.DS_Store  # macOS
Thumbs.db  # Windows

# Resume is synced from latex-resume releases (see .github/workflows/resume-sync.yml)

# Backup files (from validate script)
*.backup-*

# Environment files
.env
.env.local
.env.*.local

# Node modules (if you add any JS tooling)
node_modules/

# Python cache (if you add any Python scripts)
__pycache__/
*.pyc
