# VS Code settings
.vscode/
!.vscode/settings.json  # Keep settings but ignore others

# Markdown linting configuration (keep local)
.markdownlint.json

# Temporary files
*.tmp
*.temp
*.log
*.cache
*.bak
*.old
*~
.DS_Store  # macOS
Thumbs.db  # Windows

# Resume upload staging (allow PDFs so the versioning workflow can trigger)
resume/upload/*
!resume/upload/.gitkeep
!resume/upload/*.pdf

# Backup files (from validate script)
*.backup-*

# Environment files
.env
.env.local
.env.*.local

# Node modules (if you add any JS tooling)
node_modules/

# Python cache (if you add any Python scripts)
__pycache__/
*.pyc
