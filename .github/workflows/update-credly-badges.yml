# Credly Badges Auto-Update Workflow
# Uses Sagargupta16/credly-badge-readme-action to fetch and update badges
name: Update Credly Badges

on:
  schedule:
    - cron: "0 9 * * 1" # Every Monday at 9 AM UTC
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Dry run (show changes without committing)"
        required: false
        default: false
        type: boolean

permissions:
  contents: write

jobs:
  update-badges:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Install yq (YAML parser)
        run: |
          sudo wget -qO /usr/local/bin/yq https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64
          sudo chmod +x /usr/local/bin/yq

      - name: Read Credly username from config
        id: config
        run: |
          CREDLY_USER=$(yq '.credly.username' .github/config/profiles.yml)
          if [ -z "$CREDLY_USER" ] || [ "$CREDLY_USER" = "null" ]; then
            echo "::error::Failed to read credly.username from profiles.yml"
            exit 1
          fi
          echo "credly_user=$CREDLY_USER" >> $GITHUB_OUTPUT
          echo "Credly username: $CREDLY_USER"

      - name: Update Credly badges
        id: credly
        uses: Sagargupta16/credly-badge-readme-action@7ffd35098950b3649eb545322672388a5b19cd20 # v1.1.0
        with:
          credly-username: ${{ steps.config.outputs.credly_user }}
          # the block lives in a data file; scripts/render-svgs.py draws it as assets/svg/certs.svg
          readme-path: assets/credly-badges.md
          # also draw every badge as the certifications card the README shows
          output: both
          svg-path: assets/svg/certs.svg

      - name: Show diff
        run: |
          if git diff --quiet assets/credly-badges.md; then
            echo "No badge changes detected."
          else
            echo "Changes detected:"
            git diff assets/credly-badges.md
          fi

      - name: Commit changes
        # the action's changed output was never set, so this step never ran; git diff decides instead
        if: ${{ github.event.inputs.dry_run != 'true' }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add assets/credly-badges.md assets/svg/certs.svg
          if ! git diff --cached --quiet; then
            git commit -m "chore: update Credly badges"
            git push
          fi

      - name: Dry run notice
        if: ${{ github.event.inputs.dry_run == 'true' }}
        run: echo "::notice::Dry run mode - changes were NOT committed"
